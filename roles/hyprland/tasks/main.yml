---
- include_role:
    name: 0-common
  vars:
    packages: 
      - hyprland-devel
      - hyprlock
      - hypridle
    config_dir_name: hypr
    config_files:
      - hyprland.conf
      - hyprlock.conf
      - hypridle.conf

- name: Install hyprshot
  include_tasks:
    file: hyprshot.yml